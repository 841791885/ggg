server:
  address: ":8080"
  read_header_timeout: 5s
  shutdown_timeout: 10s

database:
  host: "127.0.0.1"
  port: 3307
  username: "gomall"
  password: "gomall_dev"
  name: "gomall"
  charset: "utf8mb4"
  collation: "utf8mb4_0900_ai_ci"
  max_open_connections: 20
  max_idle_connections: 10
  connection_max_lifetime: 30m
  connection_max_idle_time: 5m
  connect_timeout: 5s

auth:
  jwt_secret: "learning-gomall-secret-change-me"
  token_ttl: 2h

payment:
  # 模拟渠道回调的 HMAC 验签密钥（学习用；生产经环境变量注入，不入库）
  callback_secret: "mock-channel-secret-dev-only"

log:
  environment: "development"
  level: "debug"

worker:
  # A3 后台任务消费者：超时关单 + 站内信投递。调试 HTTP 时可临时关掉避免数据被自动改动
  enabled: true
  scan_interval: 5s
